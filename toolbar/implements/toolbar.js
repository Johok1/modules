export default class Toolbar {
    registerElement = () => {
        console.error("function registerElement must be defined for type ToolbarInterface")
    }

    constructToolbar = () => {
        console.error("funciton constructToolbar must be defined for type ToolbarInterface")
    }
}